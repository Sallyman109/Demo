Please configure the correct URLs in the app.config of the Wpf Application

Wpf GUI accepts one cost/payment per user at a time. However whil testing with Postman or ARC or any other REST client, you can pass in multiple costs per user.

REST API has to be deployed either to IIS Express or IIS

All Data represented by Models are store in RAM